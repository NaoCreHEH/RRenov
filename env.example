# Fichier d'exemple pour les variables d'environnement

# Configuration de la base de données (MySQL/TiDB)
# Remplacer les valeurs par vos informations de connexion réelles
DATABASE_URL="mysql://user:password@host:port/database"

# Clé secrète pour les tokens JWT (doit être longue et aléatoire)
JWT_SECRET="votre_cle_secrete_jwt_longue_et_aleatoire"

# OpenID de l'utilisateur administrateur (pour l'authentification simple)
# Utilisé pour initialiser le compte admin
OWNER_OPEN_ID="votre_openid_admin_unique"

# Variables pour le service de notification (si vous utilisez le système Manus)
# Ces variables sont généralement fournies par l'environnement d'hébergement (Render)
# Si vous n'utilisez pas le système de notification intégré, vous pouvez les laisser vides
# ou les remplacer par les variables de votre propre service d'envoi d'e-mail (ex: SENDGRID_API_KEY)
BUILT_IN_FORGE_API_URL=""
BUILT_IN_FORGE_API_KEY=""

# Configuration pour l'authentification OAuth (si utilisée)
OAUTH_SERVER_URL=""
